"""Services layer.

Import submodules directly (``from bot.services import stats``); this package
deliberately stays side-effect free so that ``bot.context`` can import a single
service without dragging in the whole graph at startup.
"""
